info:
  name: Post Customer Missing Required Fields
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/v1/customers
  body:
    type: json
    data: |-
      {
        "city": "Austin",
        "membership_level": "Silver"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
