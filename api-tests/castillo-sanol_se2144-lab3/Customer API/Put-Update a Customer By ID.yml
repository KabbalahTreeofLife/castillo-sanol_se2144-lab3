info:
  name: Put/Update a Customer By ID
  type: http
  seq: 1

http:
  method: PUT
  url: http://localhost:3000/api/v1/customers/C106
  body:
    type: json
    data: |-
      {
        "city": "Seattle",
        "membership_level": "Gold"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
